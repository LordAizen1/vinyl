{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. core:window:allow-start-dragging is not part of core:default: that set grants 28 window permissions and every one is a read (allow-is-*, allow-get-*, allow-scale-factor). Anything that moves or changes a window must be named explicitly, so without this line startDragging is denied and the chassis does not drag.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging"
  ]
}
